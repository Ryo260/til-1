* :Tutorial
* オペレータ／カウント／モーション
  * オペレータ  
  指定なし → 移動
  [d] → 削除
  
  * モーション  
  [w] → 次の単語の先頭まで
  [e] → 次の単語の末尾まで
  [$] → カーソルがある行の末尾まで
  [0] → カーソルがある行の先頭まで
  
  * カウント  
  カウント + モーションで、そのモーションの動作を複数回繰り返す指定となる。  
  2w → 次の次の単語へ移動  
  d2w → 次の次の単語まで削除